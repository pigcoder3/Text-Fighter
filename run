#!/bin/bash
CURRDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
cd $CURRDIR
javac -classpath ".:lib/JSONparser/json-20180813.jar" -d . src/org/tfighter/*.java
java -classpath ".:lib/JSONparser/json-20180813.jar" tfighter.TextFighter
